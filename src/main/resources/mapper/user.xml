<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.ppoddolog.domain.user.UserDao">

	<select id="findById"
		resultType="com.example.ppoddolog.domain.user.User">
		SELECT * FROM user WHERE user_id=#{userId}
	</select>

</mapper>